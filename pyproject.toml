[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # long lines in Glue job argument dicts are unavoidable

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
